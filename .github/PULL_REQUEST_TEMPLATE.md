## Summary
What this PR changes and why.

## Checklist
- [ ] If I edited templates, I ran `npm run sync` (and `npm run sync:check` passes)
- [ ] `npm run check` passes locally (sync:check + validate + smoke + tests)
- [ ] Commit messages reference an arc where applicable (`[ARC-NNNN] …`)
